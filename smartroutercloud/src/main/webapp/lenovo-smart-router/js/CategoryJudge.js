function categoryWebName(obj){
			var arr = new Array(0);
			var appImageName="";
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="sns"){
						arr.push(obj.length-j-1);	
					}
				}	
			}	
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){		
					appImageName=appImageName.toLowerCase();
					if(appImageName=="video"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="social"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="news"){
						arr.push(obj.length-j-1);	
					}
				}

			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="shopping"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="music"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="trip"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="life-service"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="game"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="e-radio"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="security"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="dev-manage"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="portal"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="military"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="sports"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="finance"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="tech"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="mobile"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="ent"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="auto"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="blog"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="house"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="book"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="edu"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="fashion"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="health"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="lottery"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="art"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="pet"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="history"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="culture"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="society"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="astro"){
						arr.push(obj.length-j-1);	
					}
				}
			}
			for (var j=0;j<5;j++) {
				if(obj[obj.length-j-1]==""||obj[obj.length-j-1]=="Unknown"||obj[obj.length-j-1]=="unknown"){
					arr.push(obj.length-j-1);	
				}
			}

			return arr; 

}
function categoryAppName(obj){
			var arrApp = new Array(0);
			var appImageName="";
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="sns"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="video"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="social"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="news"){
						arrApp.push(obj.length-j-1);	
					}
				}

			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="shopping"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="music"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="trip"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="life-service"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="game"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="e-radio"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="security"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="dev-manage"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="portal"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="military"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="sports"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="finance"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="tech"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="mobile"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="ent"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="auto"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="blog"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="house"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="book"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="edu"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="fashion"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="health"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="lottery"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="art"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="pet"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="history"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="culture"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="society"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var	 j=0;j<5;j++) {
				appImageName=obj[obj.length-j-1];
				if(appImageName){
					appImageName=appImageName.toLowerCase();
					if(appImageName=="astro"){
						arrApp.push(obj.length-j-1);	
					}
				}
			}
			for (var j=0;j<5;j++) {
				if(obj[obj.length-j-1]==""||obj[obj.length-j-1]=="Unknown"||obj[obj.length-j-1]=="unknown"){
					arrApp.push(obj.length-j-1);	
				}
			}
			return arrApp;

}